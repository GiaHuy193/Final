@page
@model ChangePasswordModel
@{
    ViewData["Title"] = "Đổi mật khẩu";
    ViewData["ActivePage"] = ManageNavPages.ChangePassword;
}

<style>
    /* Dùng chung hệ biến màu với trang Index */
    :root {
        --primary-color: #258cf4;
        --bg-light: #f8f9fa;
        --border-color: #e9ecef;
    }

    .profile-container {
        background-color: var(--bg-light);
        min-height: 100vh;
        padding: 3rem 0;
    }

    .card {
        border: none;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.04);
    }

    .form-label {
        color: #495057;
        margin-bottom: 8px;
    }

    .form-control {
        border-radius: 12px;
        padding: 14px;
        border: 1px solid var(--border-color);
        background-color: white;
    }

        .form-control:focus {
            box-shadow: 0 0 0 0.25rem rgba(37, 140, 244, 0.1);
            border-color: var(--primary-color);
        }

    .breadcrumb-item a {
        color: var(--primary-color);
        font-weight: 500;
        text-decoration: none;
    }

    .security-icon-bg {
        width: 60px;
        height: 60px;
        background: rgba(37, 140, 244, 0.1);
        color: var(--primary-color);
        border-radius: 15px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 20px;
    }
</style>

<div class="profile-container">
    <div class="container">
        <div class="row mb-5 justify-content-center">
            <div class="col-lg-10">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/">DocNest</a></li>
                        <li class="breadcrumb-item"><a asp-page="./Index">Hồ sơ</a></li>
                        <li class="breadcrumb-item active">Bảo mật</li>
                    </ol>
                </nav>
                <h1 class="fw-bold mb-1">Thiết lập bảo mật</h1>
                <p class="text-muted mb-0">Cập nhật mật khẩu định kỳ để bảo vệ tài khoản DocNest của bạn.</p>
            </div>
        </div>

        <div class="row g-4 justify-content-center">
            <div class="col-lg-6">
                <div class="card p-4 p-md-5 h-100">
                    <div class="d-flex align-items-center gap-3 mb-4">
                        <div class="security-icon-bg">
                            <i class="bi bi-shield-lock-fill fs-3"></i>
                        </div>
                        <h4 class="fw-bold mb-0">Thay đổi mật khẩu</h4>
                    </div>

                    <partial name="_StatusMessage" for="StatusMessage" />

                    <form id="change-password-form" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger small mb-3" role="alert"></div>

                        <div class="mb-4">
                            <label asp-for="Input.OldPassword" class="form-label small fw-bold text-secondary">Mật khẩu hiện tại</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-key text-muted"></i></span>
                                <input asp-for="Input.OldPassword" class="form-control" autocomplete="current-password" aria-required="true" placeholder="Nhập mật khẩu cũ" />
                            </div>
                            <span asp-validation-for="Input.OldPassword" class="text-danger small"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Input.NewPassword" class="form-label small fw-bold text-secondary">Mật khẩu mới</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-lock-fill text-muted"></i></span>
                                <input asp-for="Input.NewPassword" class="form-control" autocomplete="new-password" aria-required="true" placeholder="Nhập mật khẩu mới" />
                            </div>
                            <span asp-validation-for="Input.NewPassword" class="text-danger small"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Input.ConfirmPassword" class="form-label small fw-bold text-secondary">Xác nhận mật khẩu mới</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-0"><i class="bi bi-check-all text-muted"></i></span>
                                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" aria-required="true" placeholder="Nhập lại mật khẩu mới" />
                            </div>
                            <span asp-validation-for="Input.ConfirmPassword" class="text-danger small"></span>
                        </div>

                        <div class="pt-2">
                            <button type="submit" class="btn btn-primary w-100 py-3 rounded-pill fw-bold shadow-sm">
                                <i class="bi bi-arrow-repeat me-2"></i>Cập nhật mật khẩu mới
                            </button>
                            <a asp-page="./Index" class="btn btn-link w-100 text-secondary text-decoration-none mt-2 small">Quay lại hồ sơ</a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card p-4 p-md-5 bg-white border border-light h-100 shadow-lg">
                    <h5 class="fw-bold mb-4">Gợi ý bảo mật</h5>

                    <div class="d-flex gap-3 mb-4">
                        <i class="bi bi-check-circle-fill text-success fs-5"></i>
                        <div>
                            <h6 class="fw-bold mb-1 small">Độ dài tối thiểu</h6>
                            <p class="text-muted small mb-0">Mật khẩu nên có ít nhất 6-8 ký tự.</p>
                        </div>
                    </div>

                    <div class="d-flex gap-3 mb-4">
                        <i class="bi bi-check-circle-fill text-success fs-5"></i>
                        <div>
                            <h6 class="fw-bold mb-1 small">Ký tự đặc biệt</h6>
                            <p class="text-muted small mb-0">Nên bao gồm chữ hoa, chữ thường, số và ký tự (!@@#$%...).</p>
                        </div>
                    </div>

                    <div class="mt-auto p-4 bg-light rounded-4">
                        <p class="small text-secondary mb-0">
                            <i class="bi bi-info-circle me-1"></i>
                            Nếu bạn quên mật khẩu cũ, hãy sử dụng chức năng <b>Quên mật khẩu</b> tại trang Đăng nhập.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}