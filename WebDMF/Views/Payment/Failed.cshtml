@{
    ViewData["Title"] = "Thanh toán thất bại";
}

<style>
    .card-rounded-xl {
        border-radius: 30px;
        border: none;
    }

    /* Icon lỗi màu đỏ */
    .icon-box-danger {
        width: 100px;
        height: 100px;
        background: #fee2e2; /* Đỏ nhạt */
        color: #ef4444; /* Đỏ đậm */
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto;
        font-size: 3rem;
        animation: shake 0.5s cubic-bezier(.36,.07,.19,.97) both;
    }

    /* Hiệu ứng rung lắc báo lỗi */
    @@keyframes shake {
        10%, 90% {
            transform: translate3d(-1px, 0, 0);
        }

        20%, 80% {
            transform: translate3d(2px, 0, 0);
        }

        30%, 50%, 70% {
            transform: translate3d(-4px, 0, 0);
        }

        40%, 60% {
            transform: translate3d(4px, 0, 0);
        }
    }

    .btn-rounded {
        border-radius: 50px;
        padding: 12px 30px;
        font-weight: 600;
    }
</style>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card card-rounded-xl shadow-lg p-4 text-center">
                <div class="card-body">
                    <div class="icon-box-danger mb-4">
                        <i class="bi bi-x-lg"></i>
                    </div>

                    <h2 class="fw-bold text-danger mb-3">Thanh toán thất bại</h2>
                    <p class="text-muted mb-4">
                        Rất tiếc, đã có lỗi xảy ra trong quá trình xử lý giao dịch. Tài khoản của bạn <strong>chưa bị trừ tiền</strong>.
                    </p>

                    <div class="bg-light p-3 rounded-4 mb-4 text-start">
                        <h6 class="fw-bold small text-dark mb-2"><i class="bi bi-info-circle me-1"></i> Nguyên nhân có thể:</h6>
                        <ul class="text-muted small mb-0 ps-3">
                            <li>Số dư tài khoản không đủ.</li>
                            <li>Kết nối mạng bị gián đoạn.</li>
                            <li>Hệ thống PayPal đang bảo trì.</li>
                        </ul>
                    </div>

                    <div class="d-grid gap-2">
                        <a asp-action="Premium" class="btn btn-danger btn-rounded shadow-sm">
                            <i class="bi bi-arrow-counterclockwise me-2"></i>Thử thanh toán lại
                        </a>
                        <a href="/" class="btn btn-outline-secondary btn-rounded border-0">Về trang chủ</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>