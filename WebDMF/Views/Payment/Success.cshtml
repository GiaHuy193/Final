@{
    ViewData["Title"] = "Thanh toán thành công";
}

<style>
    .card-rounded-xl {
        border-radius: 30px;
        border: none;
    }

    .icon-box {
        width: 100px;
        height: 100px;
        background: #d1e7dd;
        color: #198754;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto;
        font-size: 3rem;
        animation: popIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    @@keyframes popIn {
        from {
            transform: scale(0);
            opacity: 0;
        }

        to {
            transform: scale(1);
            opacity: 1;
        }
    }

    .btn-rounded {
        border-radius: 50px;
        padding: 12px 30px;
        font-weight: 600;
    }
</style>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card card-rounded-xl shadow-lg p-4 text-center">
                <div class="card-body">
                    <div class="icon-box mb-4">
                        <i class="bi bi-check-lg"></i>
                    </div>

                    <h2 class="fw-bold text-success mb-3">Thanh toán thành công!</h2>
                    <p class="text-muted mb-4">
                        Chúc mừng bạn! Tài khoản đã được nâng cấp lên gói <span class="fw-bold text-dark">Premium (100GB)</span>.
                    </p>

                    <div class="bg-light p-3 rounded-4 mb-4 text-start">
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-muted small">Gói dịch vụ:</span>
                            <span class="fw-bold">Premium Monthly</span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-muted small">Thanh toán qua:</span>
                            <span class="fw-bold"><i class="bi bi-paypal me-1"></i>PayPal</span>
                        </div>
                        <div class="d-flex justify-content-between">
                            <span class="text-muted small">Trạng thái:</span>
                            <span class="badge bg-success rounded-pill">Đã kích hoạt</span>
                        </div>
                    </div>

                    <div class="d-grid gap-2">
                        <a asp-area="Identity" asp-page="/Account/Manage/Index" class="btn btn-primary btn-rounded shadow-sm">
                            Kiểm tra dung lượng ngay
                        </a>
                        <a href="/" class="btn btn-link text-decoration-none text-muted">Về trang chủ</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>